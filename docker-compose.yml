version: '3.8'

services:
  frontend:
    container_name: internship_frontend
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run serve
    ports:
      - '8080:8080'
    volumes:
      - .:/app
      - /app/node_modules
